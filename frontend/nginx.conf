http {
    server {
        listen 80;

        location / {
            root /srv/site;
            
        }

        location /api {
            proxy_pass http://backend:8081/api;
        }
    }
}

events {}